{
	"name": "trans-bot",
	"private": true,
	"version": "0.0.1",
	"description": "Telegram bot with AI capabilities",
	"scripts": {
		"build": "pnpm -r build",
		"dev": "pnpm -r dev",
		"test": "pnpm -r test",
		"clean": "pnpm -r clean",
		"typecheck": "pnpm -r typecheck"
	},
	"engines": {
		"node": ">=18.0.0",
		"pnpm": ">=8.0.0",
		"bun": ">=1.0.0"
	},
	"packageManager": "pnpm@8.15.4",
	"workspaces": [
		"packages/*"
	],
	"devDependencies": {
		"@types/node": "^22.14.1"
	}
}
